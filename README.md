This utility webapp can be used to access a system terminal from any device over the same network.

To make this app available over the entire network:
export FLASK_APP=webapp.py
flask run -h Ip.add.rr.ess
